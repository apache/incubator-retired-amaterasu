language: scala

cache:
  directories:
  - $HOME/.m2
  - $HOME/.gradle
  - ~/.gradle
install:
  - ./gradlew buildDistribution
deploy:
-
 provider: releases
 skip_cleanup: true
 file_glob: true
 file: "sdk/build/libs/*"
 on:
   repo: annoymousGiraf/incubator-amaterasu
   
notifications:
  email:
    - yaniv@apache.org
